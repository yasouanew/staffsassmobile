<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Android 13 (API 33)+ requires a runtime permission before an app may post
      notifications. `requestPermission` in src/services/push/pushService.ts prompts for
      this; without the manifest declaration the prompt is never shown and the call
      returns "denied" silently.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!--
        Default notification channel for FCM messages that arrive with a `notification`
        payload but no explicit `channel_id`. The channel itself is created at runtime in
        pushService (see ensureAndroidNotificationChannel), because a channel defined only
        in the manifest is never actually registered with the OS. Its id MUST match
        FCM_ANDROID_CHANNEL_ID in .env.
      -->
      <meta-data
        android:name="com.google.firebase.messaging.default_notification_channel_id"
        android:value="@string/default_notification_channel_id"
        tools:replace="android:value" />
    </application>
</manifest>
